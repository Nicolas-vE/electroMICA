# Core dependencies
numpy>=1.21.5
scipy>=1.7.3
pandas>=1.4.4
nibabel>=4.0.2
# ANTsPy (used as `ants` in the code). Install as `antspyx` if using pip.
antspyx>=0.3.0

# Notes:
# - Some packages (ANTsPy/ANTs) may require system-level dependencies.
# - Pin exact versions according to your environment and CI requirements.
